/* global test, expect */

import { Kind } from 'graphql/language';

import { URL } from '../';

// Number greater than zero
test('URL - TBD - serialize', () => {
  // expect(URL.serialize(123.45)).toBe(123.45);
  expect(1).toBe(2);
});

test('URL - TBD - parseLiteral', () => {
  // expect(() => URL.parseLiteral({ value: 'not a number', kind: Kind.STRING })).toThrow(/Can only validate floating point numbers as positive floating point numbers but got a/);
  expect(1).toBe(2);
});
